DeviceProcessEvents
| where ProcessCommandLine contains "lsass" or FileName in~ ("mimikatz.exe", "procdump.exe")
| where Timestamp > ago(7d)
| project Timestamp, DeviceName, FileName, ProcessCommandLine, InitiatingProcessFileName
